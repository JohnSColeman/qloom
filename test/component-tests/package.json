{
  "name": "@qloom/component-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test --grep-invert @red-pending",
    "test:pending": "playwright test --grep @red-pending",
    "test:all": "playwright test"
  },
  "dependencies": {
    "@qloom/components": "workspace:*",
    "@qloom/core": "workspace:*",
    "@qloom/router": "workspace:*",
    "@qloom/runtime": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "create-qloom": "workspace:*",
    "typescript": "^5.6.3",
    "vite": "8.0.5"
  }
}
