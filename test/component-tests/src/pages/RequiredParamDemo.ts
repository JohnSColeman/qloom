import { Page } from "@qloom/runtime";

/** Renders <t:needsparam/> with its required `label` left unbound — the render
 *  must throw (enforced in renderComponent). */
export class RequiredParamDemo extends Page {}
