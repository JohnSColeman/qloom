import { Page } from "@qloom/runtime";

/** A plain page with a link to the redirector — the entry we must be able to
 *  return to with Back after passing through a redirect. */
export class RedirectStart extends Page {}
