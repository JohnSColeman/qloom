import { Page } from "@qloom/runtime";

/**
 * tapestry: AjaxTests#progressive_display — ProgressiveDisplay renders simplified
 * initial content, then an Ajax request supplies the true body.
 */
export class ProgressiveDisplayDemo extends Page {}
