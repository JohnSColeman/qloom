import { Page, Property } from "@qloom/runtime";

/**
 * tapestry: OutputTest — Output formats a value (wrapping it in the element it is
 * placed on).
 */
export class OutputDemo extends Page {
  @Property amount = 42;
}
